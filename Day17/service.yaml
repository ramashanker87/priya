apiVersion: v1
kind: Service
metadata:
  name: priya-ecr-service
  namespace: day17-priya
spec:
  type: LoadBalancer
  selector:
    app: priya-ecr-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
