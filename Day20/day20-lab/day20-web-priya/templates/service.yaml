apiVersion: v1
kind: Service
metadata:
  name: {{ include "day20-web-priya.fullname" . }}
  labels:
    {{- include "day20-web-priya.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "day20-web-priya.selectorLabels" . | nindent 4 }}
